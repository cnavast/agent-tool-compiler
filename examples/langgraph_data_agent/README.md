# LangGraph Data Agent Demo

This demo creates a local SQLite shipments database, runs a LangGraph ReAct agent with analytics tools, and lets ATC compile a successful SQL workflow into a reusable capability.

Run from the repository root:

```bash
uv run python examples/langgraph_data_agent/server.py
```

Then open http://localhost:8000.
